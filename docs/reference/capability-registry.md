# Capability Registry Reference

`CapabilityRegistry` stores `ModelCard` entries.

`ModelCard` fields include:

- provider;
- model id;
- declared capabilities;
- adapter status;
- source;
- limitations.

A missing capability is treated as unsupported.

