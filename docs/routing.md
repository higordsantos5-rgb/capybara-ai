# Routing

Routing checks:

1. provider supported by framework;
2. provider enabled by project;
3. provider configured with required credentials;
4. adapter status allowed by policy;
5. model known in registry;
6. model enabled by project;
7. model compatible with required capabilities;
8. model/provider available at runtime.

Fallback is disabled unless explicitly allowed.

